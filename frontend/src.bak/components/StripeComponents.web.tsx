import { View } from 'react-native';

// Web stubs for Stripe native-only components
export const CardField = View;
export const useStripe = () => ({});
export const useConfirmSetupIntent = () => ({});
