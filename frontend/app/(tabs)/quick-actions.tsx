import { View } from 'react-native';

// This is a placeholder screen that should never be displayed
// The button opens a modal instead of navigating here
export default function QuickActions() {
  return <View />;
}
