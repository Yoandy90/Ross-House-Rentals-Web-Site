import React from 'react';
import { View, Text } from 'react-native';

// Web version - not used, NativePaymentModal is used instead for web
export function CreditPurchaseModal(props: any) {
  return (
    <View>
      <Text>This component is not available on web. Please use NativePaymentModal instead.</Text>
    </View>
  );
}
