# ═══════════════════════════════════════════════════════════════
# Production environment variables — Ross House Rentals (Vercel)
# ═══════════════════════════════════════════════════════════════
# Only PUBLIC variables (NEXT_PUBLIC_*) are safe to commit here.
# For secret keys, use Vercel dashboard → Settings → Environment Variables.

# Cloudflare Turnstile — public site key (safe to commit).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADtnBEHJFIjSe23R
